# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / Vite
node_modules/
frontend/dist/
frontend/.vite/

# Server runtime artifacts (local tunnel + logs — never commit)
server/tools/
*.log*

# GenLayer tooling artifacts
artifacts/
gltest.config.yaml

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local opencode configuration / skills (not part of the product)
.opencode/